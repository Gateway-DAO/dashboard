import { PrivateDataAsset } from '@/services/protocol/types';
import { DeepPartial } from 'react-hook-form';

type Props = {
  pdas: DeepPartial<PrivateDataAsset>[];
};

export default function SentPdasTable({ pdas }: Props) {
  return <>bora</>;
}
