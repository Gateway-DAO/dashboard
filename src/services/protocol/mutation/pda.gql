mutation changePDAStatus($id: String!, $status: PDAStatus!) {
  changePDAStatus(input: { id: $id, status: $status }) {
    id
    arweaveUrl
    dataAsset {
      title
      claim
    }
    status
  }
}
