query dataModelById($id: String!) {
  dataModel(id: $id) {
    title
    description
    tags
    createdBy {
      ...min_user
    }
    organization {
      ...org
    }
    schema
    consumptionPrice
  }
}

query dataModels($user: UserIdentificationInput, $skip: Int, $take: Int) {
  dataModels(filter: { user: $user }, skip: $skip, take: $take) {
    id
    title
    createdAt
  }
}

query dataModelsByOrg(
  $organization: OrganizationIdentificationInput
  $skip: Int
  $take: Int
) {
  dataModels(
    filter: { organization: $organization }
    skip: $skip
    take: $take
  ) {
    id
    title
    createdAt
  }
}

query dataModelsCount($user: UserIdentificationInput) {
  dataModelsCount(filter: { user: $user })
}

query dataModelsCountOrg($organization: OrganizationIdentificationInput) {
  dataModelsCount(filter: { organization: $organization })
}
