export const REQUIRED_MIN_LENGTH = (minLength: number) =>
  `This field is required and must contain at least ${minLength} characters`;
export const REQUIRED_MAX_LENGTH = (maxLength: number) =>
  `This field is required and must contain at most ${maxLength} characters`;
