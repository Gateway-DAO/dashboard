import { GatewaySxProps } from '@/theme';
import { NEGATIVE_CONTAINER_PX } from '@/theme/config/style-tokens';

export const pageWithBackgroundColor: GatewaySxProps = {
  mx: NEGATIVE_CONTAINER_PX,
  mt: {
    xs: -2,
    lg: -5,
  },
  mb: {
    xs: -10,
    lg: -4,
  },
};
